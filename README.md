# 🚀 EntryPoint — Агрегатор IT-стажировок

<p align="center">
  <strong>Твоя первая работа начинается здесь</strong>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/TypeScript-5.x-3178C6?style=for-the-badge&logo=typescript&logoColor=white" alt="TypeScript">
  <img src="https://img.shields.io/badge/Vite-5.x-646CFF?style=for-the-badge&logo=vite&logoColor=white" alt="Vite">
  <img src="https://img.shields.io/badge/CSS-Vanilla-1572B6?style=for-the-badge&logo=css3&logoColor=white" alt="CSS">
  <img src="https://img.shields.io/badge/Деплой-Vercel-000?style=for-the-badge&logo=vercel&logoColor=white" alt="Vercel">
</p>

---

## 📌 О проекте

**EntryPoint** — это веб-приложение (SPA), предназначенное для поиска IT-стажировок студентами российских ВУЗов. Сервис агрегирует вакансии стажировок и предоставляет удобную систему фильтрации, позволяя за пару кликов найти подходящую позицию и откликнуться через Telegram или email.

### Что умеет EntryPoint

- 🔍 **Полнотекстовый поиск** — по названию, компании и описанию вакансии
- 🏷️ **Гибкая фильтрация** — по направлению (Backend, Frontend, Data, AI, DevOps…), стеку технологий, ВУЗу, городу, формату работы, оплате
- 🎓 **Профильные фильтры** — по курсу обучения, GPA, минимальной зарплате
- 📄 **Детальный просмотр вакансий** — требования, описание, условия, зарплатная вилка
- 📨 **Быстрый отклик** — переход в Telegram или отправка email напрямую
- 🔗 **Шеринг** — копирование ссылки на вакансию одной кнопкой
- ✍️ **Создание анкеты** — работодатель может разместить вакансию через форму (сохраняется в localStorage)
- 🖥️ **Matrix-эффект** — анимированный фон в стиле «Матрицы» через Canvas API

---

## 🖼️ Дизайн

Сайт выполнен в тёмном стиле с акцентами зелёного неона и эффектами **glassmorphism** (матовое стекло с `backdrop-filter: blur`). Фоном служит анимация Matrix Rain.

| Элемент | Стиль |
|---------|-------|
| Фон | `#070a0a` + Matrix Rain (Canvas / iframe) |
| Акцент | `#56ff8a` (неоновый зелёный) |
| Панели | Glassmorphism (`rgba(12, 18, 18, 0.65)` + blur 12px) |
| Шрифты | Inter (UI), JetBrains Mono (теги, код) |
| Кнопки | Hover lift (`translateY(-2px)`) + glow |

---

## 🛠️ Технологический стек

| Компонент | Технология |
|-----------|------------|
| Язык | **TypeScript 5.x** |
| Сборщик | **Vite 5.x** |
| Стили | Vanilla CSS (кастомные, без фреймворков) |
| Хранение данных | LocalStorage |
| Шрифты | Inter, JetBrains Mono |
| Визуальные эффекты | Canvas API (Matrix Rain) |
| Деплой | Vercel (статический хостинг) |

---

## 🚀 Запуск

### Требования

- **Node.js** ≥ 18.x
- **npm** ≥ 9.x

### Локальный запуск

```bash
# Клонировать репозиторий
git clone git@github.com:Lemelson/EntryPoint.git
cd EntryPoint

# Установить зависимости
npm install

# Запустить dev-сервер
npm run dev
```

Vite откроет сайт по адресу **`http://localhost:5173`**.

### Production-сборка

```bash
npm run build
npm run preview   # предпросмотр на http://localhost:4173
```

---

## 📁 Структура проекта

```
EntryPoint/
├── src/                          # Исходный код приложения
│   ├── main.ts                   # Основная логика: рендер, роутинг, фильтрация, модалки
│   ├── data.ts                   # Данные: вакансии, справочники ВУЗов/городов/стека
│   ├── style.css                 # Все стили + адаптив + glassmorphism
│   ├── matrix.ts                 # Matrix-эффект для бейджа
│   └── ui.ts                     # UI-утилиты (escape, форматирование)
├── public/
│   └── rezmason-matrix/          # Вендорный Matrix-эффект (iframe-фон)
├── code4site/                    # Копия исходников (для документации)
├── СПЕЦИФИКАЦИЯ_САЙТА.md         # Подробная техническая спецификация
├── EntryPoint_Presentation.pptx  # Презентация проекта
├── index.html                    # Точка входа
├── package.json                  # Зависимости и скрипты
├── tsconfig.json                 # Конфигурация TypeScript
└── README.md                     # ← Вы здесь
```

---

## 🧭 Архитектура

**Тип приложения:** Single Page Application (SPA) с клиентским роутингом.

### Роутинг

| Маршрут | Описание |
|---------|----------|
| `#/` | Главная — список вакансий с фильтрами и поиском |
| `#/internship/:id` | Детальная страница вакансии |
| `#/*` | Страница 404 |

### Фильтрация

Система поддерживает многоуровневую фильтрацию с debounce (90мс):
- **Текстовый поиск** — по названию, компании, описанию
- **Категорийные фильтры** — направление, формат, город, оплата (множественный выбор)
- **Стек / ВУЗ** — совпадение хотя бы по одному значению
- **Профильные** — курс, GPA, минимальная зарплата

### Хранение данных

| Ключ | Описание |
|------|----------|
| `entrypoint.user_posts.v1` | Пользовательские анкеты (JSON) |
| `entrypoint.welcome.seen` | Флаг показа приветственного модального окна |

---

## 📱 Адаптивность

Сайт полностью адаптивен:

| Ширина экрана | Поведение |
|---------------|-----------|
| **> 960px** | Двухколоночный layout (фильтры слева + результаты справа) |
| **≤ 960px** | Одноколоночный layout, фильтры сверху |
| **≤ 520px** | Компактный мобильный вид, вертикальный searchbar |

---

## ♿ Доступность

- Семантическая HTML-разметка (`<main>`, `<nav>`, `<article>`)
- ARIA-атрибуты для модальных окон и кнопок
- Навигация клавиатурой (Tab, Enter, Escape)
- Поддержка `prefers-reduced-motion`

---

## 🌐 Деплой на Vercel

| Параметр | Значение |
|----------|----------|
| Root Directory | `/` (корень репозитория) |
| Build Command | `npm run build` |
| Output Directory | `dist` |
| Node Version | 18.x |

---

## 📝 Лицензия

Open Source проект.

---

<p align="center">
  <sub>Сделано с 💚 командой EntryPoint</sub>
</p>
