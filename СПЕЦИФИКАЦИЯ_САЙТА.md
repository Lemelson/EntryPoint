# 📋 Техническая спецификация сайта EntryPoint

**Версия:** 1.0  
**Дата:** 08.02.2026

---

## 1. Общее описание

### 1.1 Назначение
**EntryPoint** — веб-приложение (SPA), представляющее собой агрегатор IT-стажировок для студентов российских ВУЗов. Сервис позволяет студентам находить и фильтровать вакансии стажировок, просматривать детальную информацию и откликаться напрямую через Telegram или email.

### 1.2 Целевая аудитория
- **Студенты** IT-специальностей российских ВУЗов (1-4 курс)
- **Работодатели** — HR-специалисты и рекрутеры IT-компаний

### 1.3 Ключевые функции
| Функция | Описание |
|---------|----------|
| Поиск вакансий | Полнотекстовый поиск по названию и описанию |
| Фильтрация | По направлению, стеку, ВУЗу, городу, формату, оплате |
| Профильные фильтры | По курсу обучения, GPA, минимальной зарплате |
| Детальный просмотр | Полное описание вакансии с требованиями |
| Отклик | Переход в Telegram или email работодателя |
| Шеринг | Копирование ссылки на вакансию |
| Создание анкеты | Добавление демо-вакансии работодателем |

---

## 2. Архитектура

### 2.1 Тип приложения
**Single Page Application (SPA)** с клиентским роутингом через hash-навигацию (`#/`, `#/internship/:id`).

### 2.2 Технологический стек

| Компонент | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| Язык | TypeScript | 5.x | Типизация, безопасность кода |
| Сборщик | Vite | 5.x | Fast HMR, оптимизированная сборка |
| Стили | Vanilla CSS | - | Кастомные стили, без фреймворков |
| Хранение | LocalStorage | - | Пользовательские анкеты |
| Шрифты | Inter, JetBrains Mono | - | UI и моноширинный |
| Эффекты | Canvas API | - | Matrix-эффект фона |
| Деплой | Vercel | - | Статический хостинг |

### 2.3 Структура проекта

```
code4site/
├── src/
│   ├── main.ts          # Основная логика приложения (1400+ строк)
│   ├── style.css        # Стили (1000+ строк)
│   ├── data.ts          # Данные и справочники (inline в main.ts)
│   ├── matrix.ts        # Matrix-эффект для бейджа
│   └── ui.ts            # UI-утилиты (escape, форматирование)
├── public/
│   └── rezmason-matrix/ # Вендорный Matrix-эффект (iframe)
├── index.html           # Точка входа
├── package.json         # Зависимости
├── tsconfig.json        # Конфигурация TypeScript
└── vite.config.ts       # Конфигурация Vite
```

---

## 3. Модели данных

### 3.1 Internship (Стажировка)

```typescript
type Internship = {
  id: string;              // Уникальный идентификатор
  title: string;           // Название позиции
  company: string;         // Название компании
  logo?: string;           // URL логотипа (опционально)
  city: City;              // Код города
  direction: Direction;    // Направление (Backend, Frontend, etc.)
  format: WorkFormat;      // Формат работы (Remote, Office, Hybrid)
  paid: Paid;              // Тип оплаты (Paid, Unpaid)
  salary?: {               // Зарплата (опционально)
    min: number;
    max: number;
    currency: string;
  };
  stack: string[];         // Технологический стек
  universities: University[]; // Целевые ВУЗы
  minCourse?: number;      // Минимальный курс (1-4)
  minGpa?: number;         // Минимальный GPA (0-5)
  start: string;           // Дата старта ("ASAP" или сезон)
  duration: string;        // Длительность ("3 месяца", etc.)
  description: string;     // Краткое описание
  requirements: string[];  // Требования
  niceToHave?: string[];   // Плюсом будет
  applyTelegram?: string;  // Telegram для отклика
  applyEmail?: string;     // Email для отклика
  postedAt: string;        // Дата публикации (ISO)
  isUserPost?: boolean;    // Флаг пользовательской анкеты
};
```

### 3.2 Справочники

```typescript
// Направления разработки
type Direction = "Backend" | "Frontend" | "Data" | "AI" | "DevOps" | "Mobile" | "Security";

// Форматы работы
type WorkFormat = "Remote" | "Office" | "Hybrid";

// Тип оплаты
type Paid = "Paid" | "Unpaid";

// Города
type City = "Any" | "Moscow" | "SPb" | "Kazan" | "Novosibirsk" | "Ekaterinburg" | "Innopolis";

// ВУЗы (коды)
type University = "HSE" | "MSU" | "MIPT" | "ITMO" | "SPbGU" | "SPbPU" | "TPU" | "GUAP" | 
                  "Innopolis" | "KFU" | "REU" | "UrFU" | "TSU" | "NSU" | "Bauman" | 
                  "MEPhI" | "MISIS" | "MAI" | "MTUCI" | "MPEI";
```

### 3.3 Filters (Состояние фильтров)

```typescript
type Filters = {
  q: string;                    // Поисковый запрос
  universities: Set<University>; // Выбранные ВУЗы
  cities: Set<City>;            // Выбранные города
  directions: Set<Direction>;   // Выбранные направления
  formats: Set<WorkFormat>;     // Выбранные форматы
  paid: Set<Paid>;              // Тип оплаты
  stack: Set<string>;           // Технологии
  starts: Set<string>;          // Периоды старта
  profileGpa?: number;          // GPA пользователя
  profileCourse?: number;       // Курс пользователя (1-4)
  salaryMin?: number;           // Минимальная зарплата
};
```

---

## 4. Роутинг

### 4.1 Маршруты

| Маршрут | Компонент | Описание |
|---------|-----------|----------|
| `#/` | ListView | Главная страница со списком вакансий |
| `#/internship/:id` | DetailView | Детальная страница вакансии |
| `#/*` | NotFound | Страница 404 |

### 4.2 Реализация

```typescript
type Route =
  | { name: "list" }
  | { name: "detail"; id: string }
  | { name: "notfound" };

function parseRoute(): Route {
  const hash = location.hash || "#/";
  if (hash === "#/" || hash === "#") return { name: "list" };
  const match = hash.match(/^#\/internship\/(.+)$/);
  if (match) return { name: "detail", id: match[1] };
  return { name: "notfound" };
}
```

---

## 5. Компоненты UI

### 5.1 Структура страницы

```
┌─────────────────────────────────────────────────────────────┐
│                        HEADER                               │
│  [Logo: EntryPoint]  [Search]  [Matrix] [Intro] [Создать]   │
├─────────────────────────────────────────────────────────────┤
│                         HERO                                │
│  "Твоя первая работа начинается здесь"                      │
│  Подзаголовок + Searchbar                                   │
├───────────────────┬─────────────────────────────────────────┤
│    FILTERS        │              RESULTS                    │
│                   │                                         │
│ • Направление     │  ┌─────────────────────────────────┐    │
│ • Формат          │  │         CARD                    │    │
│ • Город           │  │  [Logo] Title        [Salary]   │    │
│ • Зарплата        │  │  Company                        │    │
│ • Начало          │  │  [Tags: stack, format, etc.]    │    │
│ • Стек            │  │  Description...                 │    │
│ • ВУЗ             │  │  [Posted] [Apply] [Share]       │    │
│ • Курс/GPA        │  └─────────────────────────────────┘    │
│                   │                                         │
│ [Сбросить]        │  [Card 2] [Card 3] ...                  │
│                   │                                         │
└───────────────────┴─────────────────────────────────────────┤
│                        FOOTER                               │
│  © 2026 EntryPoint • Open Source                            │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 Карточка вакансии (Card)

**Элементы:**
- Логотип компании (или инициалы)
- Название позиции
- Название компании
- Зарплатная вилка (badge)
- Теги: направление, формат, старт, стек
- Краткое описание (1-2 строки)
- Дата публикации
- Кнопки: "Откликнуться", "Поделиться"

### 5.3 Детальная страница (DetailView)

**Левая колонка:**
- Кнопка "Назад"
- Заголовок и компания
- Badges: ВУЗы, направление, старт
- Полное описание
- Секции: Требования, Будет плюсом

**Правая колонка:**
- KPI: Зарплата, Формат, Длительность, Город
- Кнопки: Откликнуться, Поделиться

### 5.4 Модальные окна

| Модальное окно | Триггер | Содержание |
|----------------|---------|------------|
| Welcome | Первый визит | Приветствие, описание сервиса |
| Apply | "Откликнуться" | Выбор: Telegram / Email |
| Create Post | "Создать анкету" | Многошаговая форма создания |
| Share Toast | "Поделиться" | Уведомление о копировании |

---

## 6. Логика фильтрации

### 6.1 Алгоритм

```typescript
function filteredInternships(all: Internship[], f: Filters): Internship[] {
  return all.filter((i) => {
    // 1. Текстовый поиск (title, company, description)
    if (f.q && !matchesQuery(i, f.q)) return false;
    
    // 2. Профильные фильтры
    if (f.profileCourse && i.minCourse && f.profileCourse < i.minCourse) return false;
    if (f.profileGpa && i.minGpa && f.profileGpa < i.minGpa) return false;
    if (f.salaryMin && (!i.salary || i.salary.min < f.salaryMin)) return false;
    
    // 3. Категорийные фильтры (множественный выбор)
    if (f.cities.size && !f.cities.has(i.city)) return false;
    if (f.directions.size && !f.directions.has(i.direction)) return false;
    if (f.formats.size && !f.formats.has(i.format)) return false;
    if (f.paid.size && !f.paid.has(i.paid)) return false;
    
    // 4. Стек (хотя бы одно совпадение)
    if (f.stack.size) {
      const ok = i.stack.some((t) => f.stack.has(t));
      if (!ok) return false;
    }
    
    // 5. ВУЗы (хотя бы одно совпадение)
    if (f.universities.size) {
      const ok = i.universities.some((u) => f.universities.has(u));
      if (!ok) return false;
    }
    
    // 6. Старт
    if (f.starts.size && !f.starts.has(i.start)) return false;
    
    return true;
  });
}
```

### 6.2 Debounce

Обновление результатов происходит с задержкой 90мс для оптимизации:

```typescript
function scheduleUpdateResults() {
  window.clearTimeout(updateTimer);
  updateTimer = window.setTimeout(() => updateResults(), 90);
}
```

---

## 7. Хранение данных

### 7.1 LocalStorage

| Ключ | Тип | Описание |
|------|-----|----------|
| `entrypoint.user_posts.v1` | JSON | Массив пользовательских анкет |
| `entrypoint.welcome.seen` | string | Флаг показа приветствия |

### 7.2 Миграция

```typescript
function loadUserPostsWithMigration(): Internship[] {
  // Миграция со старого ключа coolboard.user_posts.v1
  const old = localStorage.getItem(LS_OLD_POSTS);
  if (old) {
    localStorage.setItem(LS_POSTS, old);
    localStorage.removeItem(LS_OLD_POSTS);
  }
  // ...
}
```

---

## 8. Визуальный дизайн

### 8.1 Цветовая палитра

```css
:root {
  --bg: #070a0a;              /* Фон */
  --panel: rgba(12, 18, 18, 0.65); /* Панели */
  --text: #eafff3;            /* Текст */
  --accent: #56ff8a;          /* Акцент (зелёный) */
  --accent2: #20d1a1;         /* Вторичный акцент */
  --muted: rgba(234, 255, 243, 0.68); /* Приглушённый текст */
  --faint: rgba(234, 255, 243, 0.4);  /* Совсем бледный */
}
```

### 8.2 Эффекты

| Эффект | Реализация | Применение |
|--------|------------|------------|
| Glassmorphism | `backdrop-filter: blur(12px)` | Панели фильтров, результатов |
| Matrix Rain | Canvas + iframe | Фон страницы |
| Hover lift | `transform: translateY(-2px)` | Карточки, кнопки |
| Glow | `box-shadow` с accent | Активные элементы |

### 8.3 Типографика

```css
--sans: "Inter", system-ui, sans-serif;
--mono: "JetBrains Mono", "Fira Code", monospace;
```

| Элемент | Шрифт | Размер |
|---------|-------|--------|
| Заголовки | Inter Black | 24-48px |
| Основной текст | Inter | 14-16px |
| Код/теги | JetBrains Mono | 12-13px |

---

## 9. Адаптивность

### 9.1 Breakpoints

| Breakpoint | Изменения |
|------------|-----------|
| > 960px | 2-колоночный layout (filters + results) |
| ≤ 960px | 1-колоночный layout, фильтры сверху |
| ≤ 520px | Компактные чеклисты, вертикальный searchbar |

### 9.2 Примеры

```css
@media (max-width: 960px) {
  .layout { grid-template-columns: 1fr; }
  .filters { position: static; }
}

@media (max-width: 520px) {
  .searchbar { flex-direction: column; }
  .checklist { grid-template-columns: 1fr; }
}
```

---

## 10. Производительность

### 10.1 Оптимизации

| Техника | Описание |
|---------|----------|
| Debounce | Обновление фильтров с задержкой 90мс |
| RAF scheduling | Синхронизация glass-слоя через requestAnimationFrame |
| Lazy iframe | Matrix-эффект в iframe, не блокирует рендер |
| CSS containment | `contain: layout` для изолированных областей |
| Viewport clamping | Glass-слой ограничен viewport для работы blur |

### 10.2 Метрики (целевые)

| Метрика | Значение |
|---------|----------|
| First Contentful Paint | < 1.0s |
| Time to Interactive | < 1.5s |
| Lighthouse Performance | > 90 |

---

## 11. Доступность (a11y)

### 11.1 Реализованные практики

- Семантическая разметка (`<main>`, `<nav>`, `<article>`)
- ARIA-атрибуты для модальных окон (`role="dialog"`, `aria-modal="true"`)
- ARIA для кнопок-переключателей (`aria-pressed`)
- Фокус-менеджмент в модальных окнах
- Поддержка `prefers-reduced-motion` (отключение анимаций)
- Alt-тексты для изображений (через `aria-label`)

### 11.2 Keyboard navigation

| Клавиша | Действие |
|---------|----------|
| Tab | Навигация по интерактивным элементам |
| Enter/Space | Активация кнопок |
| Escape | Закрытие модальных окон |

---

## 12. SEO

### 12.1 Meta-теги

```html
<title>EntryPoint | Первая работа и стажировки</title>
<meta name="description" content="Агрегатор IT-стажировок для студентов...">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

### 12.2 Open Graph

```html
<meta property="og:title" content="EntryPoint">
<meta property="og:description" content="Найди стажировку за 2 клика">
<meta property="og:type" content="website">
```

---

## 13. Развёртывание

### 13.1 Локальный запуск

```bash
cd code4site
npm install
npm run dev
# Открыть http://localhost:5173
```

### 13.2 Production сборка

```bash
npm run build
npm run preview
```

### 13.3 Vercel деплой

| Параметр | Значение |
|----------|----------|
| Root Directory | `code4site/` |
| Build Command | `npm run build` |
| Output Directory | `dist` |
| Node Version | 18.x |

---

## 14. Известные ограничения

| Ограничение | Описание | Возможное решение |
|-------------|----------|-------------------|
| Нет бэкенда | Все данные захардкожены | Добавить API |
| LocalStorage | Ограничение 5-10 MB | IndexedDB |
| Нет авторизации | Анонимный доступ | OAuth (Google, Telegram) |
| Нет аналитики | Нет сбора статистики | Добавить Plausible/Umami |

---

## 15. Глоссарий

| Термин | Определение |
|--------|-------------|
| SPA | Single Page Application |
| Glassmorphism | Эффект "матового стекла" с blur |
| HMR | Hot Module Replacement (горячая перезагрузка) |
| Debounce | Задержка выполнения для оптимизации |
| RAF | requestAnimationFrame |

---

